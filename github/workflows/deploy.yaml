name: Deploy to EC2 with Docker

on:
  push:
    branches: [ "pb" ]  

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/amd64  # EC2 아키텍처에 맞게 빌드
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ybigta-project:latest

  deploy-to-ec2:
    name: Deploy to EC2
    needs: build-and-push  # 빌드 Job이 성공해야만 실행
    runs-on: ubuntu-latest
    steps:
      - name: SSH to EC2 and Run Container
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # 1. latest image pull
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/ybigta-project:latest
            
            # 2. 기존 컨테이너 중지, 삭제
            sudo docker rm -f my-api-server || true
            
            # 3. 새 컨테이너 실행 (볼륨 마운트, .env 필수 포함)
            sudo docker run -d \
              --name my-api-server \
              -v /home/ec2-user:/data \
              --env-file /home/ec2-user/.env \
              -p 8000:8000 \
              ${{ secrets.DOCKER_USERNAME }}/ybigta-project:latest
            
            # 4. 이미지 정리
            sudo docker image prune -f